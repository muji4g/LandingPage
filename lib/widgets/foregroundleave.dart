import 'package:flutter/material.dart';

class fgLargeleave extends StatelessWidget {
  const fgLargeleave({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'foreground_largeLv.png',
      scale: 5,
    );
  }
}
