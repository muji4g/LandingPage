import 'package:flutter/material.dart';

class BtmrtImg extends StatelessWidget {
  const BtmrtImg({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'img_1_btmrt.png',
      scale: 8,
    );
  }
}

class btmrtimgtwo extends StatelessWidget {
  const btmrtimgtwo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
