import 'package:flutter/material.dart';
import 'package:landing_page_ui/widgets/text_btn_style.dart';

class HeaderTxt extends StatelessWidget {
  const HeaderTxt({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      '420 SOCIAL PROJECT',
      style: mainText,
    );
  }
}
